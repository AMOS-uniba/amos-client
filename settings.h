#ifndef SETTINGS_H
#define SETTINGS_H

// enable this to conform to the old protocol (currently only Senec)
//#define OLD_PROTOCOL 1

// enable this to store sightings in separate directories. Should be off unless Juraj is persuaded to do otherwise
//#define SEPARATE_SIGHTINGS 1

#endif // SETTINGS_H
