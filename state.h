#ifndef STATE_H
#define STATE_H

#include "forward.h"
/*
class State {
private:
    unsigned char code;
    QString display_name;
protected:
    State(unsigned char _code, const QString& _display_name);
public:
    unsigned char get_code(void) const;
    const QString& get_display_name(void) const;
};

class CoverState: public State {
private:
    CoverState(unsigned char _code, const QString& _display_name);
public:
    static const CoverState OPEN;
    static const CoverState OPENING;
    static const CoverState CLOSED;
    static const CoverState CLOSING;
};

class FanState: public State {
private:
    FanState(unsigned char _code, const QString& _display_name);
public:
    static const FanState ON;
    static const FanState OFF;
    static const FanState UNKNOWN;
};

class IntensifierState: public State {
private:
    IntensifierState(unsigned char _code, const QString& _display_name);
public:
    static const IntensifierState ON;
    static const IntensifierState OFF;
    static const IntensifierState UNKNOWN;
};

class HotwireState: public State {
private:
    HotwireState(unsigned char _code, const QString& _display_name);
public:
    static const HotwireState ON;
    static const HotwireState OFF;
    static const HotwireState UNKNOWN;
};*/

#endif // STATE_H
